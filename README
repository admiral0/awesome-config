Installation

 git clone https://github.com/admiral0/awesome-config.git ~/.config/awesome
 cd ~/.config/awesome
 git submodule init
 git submodule update


Features:
 * Separated configuration files

TODO:
 * Battery meter (pure lua implementation)
 * Better tag management. (Idea: use shifty and name each tag after its program, then replace wibox with menu)
 * Popup XMMS2
 * Popup `screen` launcher
 * Hardware status wibar
 * separate HW related functions in separate file
